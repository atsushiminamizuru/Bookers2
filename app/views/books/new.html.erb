<!--仮作成、共通レイアウトになる！！！-->

<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :Title %>
    <h4>Opinion</h4>
      <%= f.text_area :Body %>
    <%= f.submit  %>
  <% end %>